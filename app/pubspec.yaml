name: app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  camera: ^0.12.0
  flutter:
    sdk: flutter
  flutter_tts: ^4.2.5
  http: ^1.6.0
  mobile_scanner: ^7.2.0
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  speech_to_text: ^7.3.0
  tflite_flutter: ^0.12.1
  image: ^4.2.0
  google_mlkit_barcode_scanning: ^0.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/maps/sample_building.json
    - assets/labels.txt
    - assets/yolov8n.tflite
